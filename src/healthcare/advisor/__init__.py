"""个人健康管理顾问 · 免连接器 WorkBuddy Skill 运行时。

`healthcare.advisor` wraps the local Control CLI for the WorkBuddy expert
"个人健康管理顾问": deterministic JSON commands, a per-user local data
directory, passphrase handling that never touches argv or stdout, an
independent fictional demo Vault, and the self-contained HTML workbench.
"""

VERSION = "0.1.0"
